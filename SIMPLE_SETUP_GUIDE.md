# 電気工事業務アプリ - 簡単セットアップガイド（70代の方向け）

## 🎯 このアプリでできること

- ✅ 工事案件の管理（お客様名、住所、工事内容）
- ✅ 作業リストの管理（今日やること、完了したこと）
- ✅ 見積書の記録
- ✅ 完了写真の保存
- ✅ カレンダー表示

## 📋 準備するもの

1. **パソコン**（Windows または Mac）
2. **インターネット接続**
3. **マウス**（クリック操作がしやすい）

**不要なもの：**
- スマホは不要
- 複雑な設定は不要
- 他のアプリやサービスとの連携は不要

---

## ステップ1: パソコンでの初期設定（1回だけ）

### 1-1. フォルダを開く

**Windowsの場合:**
1. デスクトップの「PC」アイコンをダブルクリック
2. 「ドキュメント」フォルダをダブルクリック
3. 「Project-Management-App_0109」というフォルダを探す

**Macの場合:**
1. Finderを開く（画面下のニコニコマークをクリック）
2. 「書類」をクリック
3. 「Project-Management-App_0109」というフォルダを探す

### 1-2. コマンドプロンプトを開く（黒い画面）

**Windowsの場合:**
1. 「Project-Management-App_0109」フォルダを開いた状態で
2. フォルダの何もないところで「Shiftキー」を押しながら「右クリック」
3. 「PowerShellウィンドウをここで開く」をクリック
   - または「コマンドウィンドウをここで開く」をクリック

**Macの場合:**
1. 「アプリケーション」→「ユーティリティ」→「ターミナル」を開く
2. 以下を入力してEnterキーを押す：
   ```
   cd ~/Documents/Project-Management-App_0109
   ```

### 1-3. 必要なプログラムをインストール

黒い画面（コマンドプロンプト）に以下を**1行ずつ**入力してEnterキーを押してください。

```bash
pnpm install
```

**画面に出る表示:**
```
Progress: ... (パーセンテージが増えていく)
Dependencies installed successfully
```

これが表示されたら成功です。**約5分かかります。**

### 1-4. データベースを作成

続けて以下を入力してEnterキーを押す：

```bash
cd apps/web
```

Enterキーを押す

```bash
echo DATABASE_URL="file:./dev.db" > .env
```

Enterキーを押す

```bash
pnpm prisma migrate dev --name init
```

Enterキーを押す

**画面に出る表示:**
```
✔ Database created successfully
✔ Migration applied successfully
```

これが表示されたら成功です。

### 1-5. 元のフォルダに戻る

```bash
cd ../..
```

Enterキーを押す

---

## ステップ2: アプリを起動する（毎回使うとき）

### 2-1. コマンドプロンプトを開く

ステップ1-2と同じ手順

### 2-2. アプリを起動

以下を入力してEnterキーを押す：

```bash
cd apps/web
pnpm dev
```

**画面に出る表示:**
```
- Local:        http://localhost:3000
- Ready in 2.3s
```

### 2-3. ブラウザでアプリを開く

**方法1: 自動的に開く場合**
- ブラウザ（Chrome、Edge、Safariなど）が自動的に開きます

**方法2: 手動で開く場合**
1. Google ChromeまたはEdgeを開く
2. アドレスバー（一番上の検索窓）に以下を入力：
   ```
   http://localhost:3000
   ```
3. Enterキーを押す

**画面に表示されるもの:**
```
┌─────────────────────────────┐
│  電気工事業務管理アプリ      │
│                             │
│  今日の作業: 3件            │
│  今月の案件: 12件           │
│                             │
│  [新規案件を登録]           │
└─────────────────────────────┘
```

---

## ステップ3: 使い方

### 案件を登録する

1. **「新規案件を登録」ボタンをクリック**

2. **情報を入力:**
   - お客様名: 「山田太郎」
   - 住所: 「東京都〇〇区...」
   - 電話番号: 「03-1234-5678」
   - 工事内容: 「エアコン取り付け」
   - 希望日: 「2026年1月15日」
   - 見積金額: 「50000」（円）

3. **「登録」ボタンをクリック**

### 案件を確認する

1. **「案件一覧」をクリック**

2. **案件をクリックすると詳細が表示される**

### 作業を完了にする

1. **案件の詳細画面で「完了」ボタンをクリック**

2. **完了日が自動で記録される**

---

## アプリを終了する方法

1. **ブラウザのタブを閉じる**

2. **黒い画面（コマンドプロンプト）で:**
   - Windowsの場合: 「Ctrl」キーと「C」キーを同時に押す
   - Macの場合: 「control」キーと「C」キーを同時に押す

3. **画面が止まったら、ウィンドウの×ボタンで閉じる**

---

## 次回使うとき

1. コマンドプロンプトを開く（ステップ1-2）
2. 以下を入力：
   ```bash
   cd apps/web
   pnpm dev
   ```
3. ブラウザで http://localhost:3000 を開く

**たったこれだけです！**

---

## トラブルシューティング

### 「pnpm: コマンドが見つかりません」と表示される

**原因:** pnpmがインストールされていない

**解決方法:**
```bash
npm install -g pnpm
```
と入力してEnterキーを押す

### ブラウザに何も表示されない

**原因:** アプリが起動していない

**解決方法:**
1. 黒い画面で `cd apps/web` と入力
2. `pnpm dev` と入力
3. 「Ready」と表示されるのを待つ

### 「ポート3000は使用中です」と表示される

**原因:** すでにアプリが起動している

**解決方法:**
1. ブラウザで http://localhost:3000 を開いてみる
2. すでに開いていたら、そのまま使える

---

## よくある質問

### Q1: 毎回コマンドを入力しないといけないの？

A: はい、アプリを使うたびに以下の3つの操作が必要です：
1. コマンドプロンプトを開く
2. `cd apps/web` と入力してEnter
3. `pnpm dev` と入力してEnter
4. ブラウザで http://localhost:3000 を開く

**もっと簡単にする方法（次のセクションで実装します）**

### Q2: インターネットがなくても使える？

A: インターネット接続は**最初の設定時のみ**必要です。
その後は、インターネットなしでも使えます。

### Q3: データはどこに保存される？

A: パソコンの中（`apps/web/dev.db`というファイル）に保存されます。
クラウドには保存されないので、安全です。

### Q4: スマホでも使える？

A: 現在の設定では使えません。
パソコンのブラウザ専用です。

---

## サポート

わからないことがあったら、この「SIMPLE_SETUP_GUIDE.md」ファイルを
サポートする人に見せてください。
